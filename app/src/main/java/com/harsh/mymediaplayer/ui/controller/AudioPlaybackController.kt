package com.harsh.mymediaplayer.ui.controller
/*

import androidx.media3.session.SessionToken
import com.harsh.mymediaplayer.PlaybackService

object AudioPlaybackController {
    private var sessionToken: SessionToken? = null
    private var currentPlayingUrl: String? = null

    fun initialize(sessionToken: SessionToken) {
        this.sessionToken = sessionToken
    }

    fun playAudio(audioUrl: String) {
        if (currentPlayingUrl != audioUrl) {
            mediaPlayerService?.pauseAudio()
            mediaPlayerService?.playAudio(audioUrl)
            currentPlayingUrl = audioUrl
            // Notify UI of audio being played
            // Update UI with new audio information
        }
    }

    fun pauseAudio() {
        mediaPlayerService?.pauseAudio()
        // Notify UI of audio being paused
    }
}
*/
